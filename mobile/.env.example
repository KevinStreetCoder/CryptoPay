# WalletConnect / Reown AppKit
EXPO_PUBLIC_WALLETCONNECT_PROJECT_ID=

# API URL (optional — defaults based on APP_ENV)
# API_URL=http://localhost:8000

# App Environment (optional — defaults to development)
# APP_ENV=development
