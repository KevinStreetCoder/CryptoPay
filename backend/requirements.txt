# Django
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0
django-filter==24.3
django-environ==0.11.2

# Database
psycopg2-binary==2.9.10

# Redis & Celery
redis==5.2.1
celery==5.4.0
django-redis==5.4.0
django-celery-beat==2.7.0

# Auth & Security
djangorestframework-simplejwt==5.4.0
bcrypt==4.2.1
cryptography==44.0.0

# Image processing
Pillow==11.2.1

# API Documentation
drf-spectacular==0.28.0

# Google OAuth
google-auth==2.37.0

# HTTP
requests==2.32.3
httpx==0.28.1

# DNS (email MX validation · Layer 2 of the disposable-email defence)
dnspython==2.6.1

# Crypto & Blockchain
tronpy==0.5.0
web3==7.6.0
bitcoinlib==0.7.7
solders==0.21.0
bit==0.8.0
mnemonic==0.21

# Rate feeds
pycoingecko==3.2.0

# SMS
africastalking==1.2.7

# 2FA / TOTP
pyotp==2.9.0
qrcode==8.0

# PDF Generation
weasyprint==68.1
# SVG → PNG rendering (for email brand assets · renders Coin-C lockup to
# PNG at deploy time so Gmail & co., which strip inline SVG, still see the
# logo via an <img src="https://cpay.co.ke/brand/…"> hosted asset).
cairosvg==2.7.1

# Monitoring
django-prometheus==2.3.1

# WebSocket / ASGI
channels==4.2.0
channels-redis==4.2.1
daphne==4.1.2

# Cloud KMS providers · KMS_PROVIDER selects which one is used at runtime.
# Both are pinned so dev environments can swap without a re-install.
boto3==1.35.0
google-cloud-kms==2.24.2

# Production
gunicorn==23.0.0
whitenoise==6.8.2
sentry-sdk[django]==2.19.2

# Dev & Testing
pytest==8.3.4
pytest-django==4.9.0
pytest-asyncio==0.24.0
factory-boy==3.3.1
faker==33.1.0
