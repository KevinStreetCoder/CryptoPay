upstream django {
    server web:8000;
}

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=general:10m rate=60r/m;

server {
    listen 80;
    server_name _;

    # Redirect HTTP to HTTPS in production
    # Uncomment when SSL is configured:
    # return 301 https://$host$request_uri;

    # For now, serve directly (development/staging)
    include /etc/nginx/conf.d/locations.conf;
}

# Uncomment for SSL:
# server {
#     listen 443 ssl http2;
#     server_name cryptopay.co.ke www.cryptopay.co.ke;
#
#     ssl_certificate /etc/letsencrypt/live/cryptopay.co.ke/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cryptopay.co.ke/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_stapling on;
#     ssl_stapling_verify on;
#
#     include /etc/nginx/conf.d/locations.conf;
# }
